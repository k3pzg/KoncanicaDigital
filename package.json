{
  "name": "koncanica-digital",
  "private": true,
  "version": "0.2.1",
  "description": "Clean foundation for KoncanicaDigital app",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "db:migrate": "npm run db:migrate --workspace backend",
    "db:seed": "npm run db:seed --workspace backend",
    "import:legacy:dry": "npm run import:legacy:dry --workspace backend",
    "import:legacy:apply": "npm run import:legacy:apply --workspace backend",
    "build": "npm run build --workspace frontend && npm run build --workspace backend",
    "cleanup:test-data:dry": "npm run cleanup:test-data:dry --workspace backend",
    "cleanup:test-data:apply": "npm run cleanup:test-data:apply --workspace backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
